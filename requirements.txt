matplotlib==3.2.1
numpy==1.18.5
# As of NumPy 1.20, certain types have been deprecated.
# Intan code has been updated for compatibility with 1.20, so if
# an updated NumPy version is desired, that can be specified here.
